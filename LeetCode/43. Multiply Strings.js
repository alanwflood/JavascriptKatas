/**
 * @param {string} num1
 * @param {string} num2
 * @return {string}
 */
var multiply = function (num1, num2) {
  console.log("=-=-=");
  let num1 = Number(num1);
  let sum = "";
  let remainder = 0;
  for (let i = nums2.length - 1; i >= 0; i--) {
    let power = num1.length - i;
    let value = num1 * :q
  }
  return sum;
};

console.log("Total:", multiply("2", "3"), "- 6");
console.log("Total:", multiply("123", "456"), "- 56088");
